import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
        flexWrap: "wrap",
    },
    button: {
      width: '97%', 
      justifyContent: "center",
      margin: 5 ,
      }
});